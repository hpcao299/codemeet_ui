import React from 'react';
import { Button } from '@mui/material';

const HomePage = () => {
    return (
        <div>
            <Button>Home</Button>
        </div>
    );
};

export default HomePage;
